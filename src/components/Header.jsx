import React from 'react';

const Header = () => {
  return (
    <header>
      <nav>
        <a href="#">Martin</a>
        <a href="#">Contáctame</a>
      </nav>
    </header>
  );
};

export default Header;
